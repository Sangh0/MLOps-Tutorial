apiVersion: apps/v1
kind: Deployment
metadata:
  name: mnist-inference
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mnist-inference
  template:
    metadata:
      labels:
        app: mnist-inference
    spec:
      containers:
        - name: mnist-inference
          image: sangho3/mnist-api:latest
          command: ["uvicorn", "app.main_inference:app", "--host", "0.0.0.0", "--port", "8000"]
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: mnist-inference-svc
spec:
  selector:
    app: mnist-inference
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
